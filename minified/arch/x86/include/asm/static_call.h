 
#ifndef _ASM_STATIC_CALL_H
#define _ASM_STATIC_CALL_H

/* ARCH_DEFINE_STATIC_CALL_*_TRAMP / ARCH_ADD_TRAMP_KEY removed - no
   DEFINE_STATIC_CALL sites exist tree-wide, so none of these arch trampoline
   macros were ever invoked (closed dead loop). */

#endif
