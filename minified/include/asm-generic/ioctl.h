#ifndef _ASM_GENERIC_IOCTL_H
#define _ASM_GENERIC_IOCTL_H

/* All ioctl-number constructor/accessor macros (_IO, _IOR, _IOW, _IOWR,
 * _IOC, _IOC_DIR/TYPE/NR/SIZE and friends) were 0-ref in this tree; dropped.
 * Header retained only as the <asm/ioctl.h> include target for fs.h/random.h. */

#endif
