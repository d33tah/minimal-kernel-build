#ifndef _LINUX_HRTIMER_H
#define _LINUX_HRTIMER_H

/* All hrtimer structs, enums, and functions removed - entire hrtimer subsystem dead.
   struct hrtimer, hrtimer_clock_base, hrtimer_cpu_base, timerqueue_node,
   timerqueue_head, enum hrtimer_base_type, enum hrtimer_restart all removed. */

#endif
